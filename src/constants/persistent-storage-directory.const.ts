import { CLASS } from "./class.const";
import { GLOBAL_PERSISTENT_STORAGE_DIRECTORY } from "./global-persistent-storage-directory.const";

export const PERSISTENT_STORAGE_DIRECTORY = `${GLOBAL_PERSISTENT_STORAGE_DIRECTORY}/instance/${CLASS}`;
